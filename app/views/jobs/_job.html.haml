.rblock
  .row
    .span3
      %h4= job.post_name
    .span9
      .rinfo
        %h5= job.company_name
        .rmeta= job.created_at.strftime("%Y-%m-%d")
        %p= raw(markdown(excerpt(job.description)))
        = link_to "Read more...", job
