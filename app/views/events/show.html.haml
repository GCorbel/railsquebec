.well
  %h1= @event.title
  %p= "Date : " + @job.created_at.strftime("%Y-%m-%d")
  %p= "Address : #{@job.street}, #{@job.postal_code}, #{@job.city}"
  - if @job.website
    %p= "Website : " + link_to(@job.website, @job.website, @job.website)
  %p= "Description : " + @job.description
